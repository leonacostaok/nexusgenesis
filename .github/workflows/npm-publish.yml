name: Publish to npm

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Run released package tests
        run: npm run test:release-packages
        timeout-minutes: 10

      # Publish in dependency order:
      # agent-keys (security core) → agent-sdk → chain-eth → chain-sol →
      # chain-adapters → mcp-server
      - name: Publish nexusgenesis-agent-keys
        working-directory: packages/agent-keys
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish nexusgenesis-agent-sdk
        working-directory: packages/agent-sdk
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish nexusgenesis-chain-eth
        working-directory: packages/chain-eth
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish nexusgenesis-chain-sol
        working-directory: packages/chain-sol
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish nexusgenesis-chain-adapters
        working-directory: packages/chain-adapters
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish nexusgenesis-agent-mcp
        working-directory: mcp-server
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  # Post-publish verification: install the ACTUALLY PUBLISHED packages from
  # the npm registry into a fresh temp dir and run the security smoke matrix
  # (agent-sdk identity/sign/verify, chain-eth Smart Account INV-005/006/007,
  # agent-mcp load). Fails the release if any published artifact is broken.
  registry-smoke:
    runs-on: ubuntu-latest
    needs: publish
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run registry smoke against freshly published packages
        run: node scripts/release-smoke.mjs
        timeout-minutes: 10
